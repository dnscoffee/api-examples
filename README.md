# DNS Coffee API Examples

Example client code for the [DNS Coffee API](https://api.dns.coffee/doc/).

The API is defined using Swagger 2.0

[Swagger schema file](https://api.dns.coffee/swagger.json).